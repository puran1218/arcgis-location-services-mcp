[project]
name = "arcgis-location-services"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "httpx>=0.28.1",
    "mcp[cli]>=1.5.0",
]

[dependency-groups]
dev = [
    "ruff>=0.11.2",
]
